const Astral = require('astral.js');
code.execute = Discord;

const bot = [
  Token: "Token du bot",
  Prefix: "Prefix du bot",
  Key: "Key d'utilisation"
 ]

bot.active.key ///Test la key d'activation

Command.handler.on = require('./commands');

[
  Nom: "ping",
  code: `$ping ms.`
 ]

